# Place Project Screenshots Here

Replace the placeholder SVG files with actual project screenshots:

- `lambda-lms.svg` (or `.png`/`.webp`) — Lambda LMS
- `skillswap.svg` — SkillSwap
- `gps-police.svg` — GPS Police Personnel Management System
- `lambda-annotate.svg` — Lambda Annotate

Recommended: WebP or optimized PNG at 1200×675px for best performance.

Update image paths in `src/data/projects.ts` if you change file extensions.
